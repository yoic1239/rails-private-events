<h1>All Created Events</h1>
<p>All events created by <%= @user.username %> are shown below.</p>

<% @user.created_events.each do |event| %>
  <div>
    <h2>Event: <%= event.name %></h2>
    <p>Date: <%= event.date %></p>
    <p>Location: <%= event.location %></p>
  </div>
<% end %>
